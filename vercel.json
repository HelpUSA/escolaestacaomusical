{
  "framework": "vite",
  "installCommand": "npm ci --include=dev",
  "buildCommand": "git lfs install && git lfs pull --include=\"public/videos/*\" --exclude=\"\" && npm run build",
  "outputDirectory": "dist"
}
